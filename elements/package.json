{
  "name": "pickit-elements",
  "version": "1.0.17",
  "description": "",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node-sass src -o build && ./node_modules/.bin/babel src --out-dir ./build",
    "prepublish": "node-sass src -o build && ./node_modules/.bin/babel src --out-dir ./build"
  },
  "author": "",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@babel/cli": "^7.5.5",
    "@babel/core": "^7.5.5",
    "@babel/preset-env": "^7.5.5",
    "@babel/preset-react": "^7.0.0",
    "babel-plugin-transform-rename-import": "^2.3.0",
    "node-sass": "^4.12.0"
  }
}
